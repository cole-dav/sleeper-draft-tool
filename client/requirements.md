## Packages
framer-motion | Smooth animations for lists and page transitions
date-fns | Formatting dates if needed
clsx | Conditional class names
tailwind-merge | Merging Tailwind classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["'Outfit'", "sans-serif"],
  body: ["'DM Sans'", "sans-serif"],
  mono: ["'JetBrains Mono'", "monospace"],
}
Sleeper API integration is handled by the backend /api/league/:id/fetch endpoint.
